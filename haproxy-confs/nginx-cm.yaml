apiVersion: v1
data:
  nginx.conf: |
    user  nginx;
    worker_processes  auto;

    error_log  /var/log/nginx/error.log notice;
    pid        /var/run/nginx.pid;

    events {
        worker_connections  10000;
    }

    http {
        include       /etc/nginx/mime.types;
        default_type  application/octet-stream;

        log_format avika_json escape=json
        '{'
          '"ts":"$time_iso8601",'
          '"req_id":"$request_id",'
          '"client":"$remote_addr",'
          '"method":"$request_method",'
          '"path":"$uri",'
          '"status":$status,'
          '"bytes":$body_bytes_sent,'
          '"rt":$request_time'
        '}';

        access_log  /var/log/nginx/access.log avika_json;

        sendfile        on;
        keepalive_timeout  65;

        map $http_upgrade $connection_upgrade {
            default upgrade;
            ''      close;
        }

        # K8s DNS Resolver
        resolver kube-dns.kube-system.svc.cluster.local valid=10s;

        server {
            listen 443 ssl;
            http2 on;
            server_name ncn112.com;

            ssl_certificate     /etc/nginx/certs/tls.crt;
            ssl_certificate_key /etc/nginx/certs/tls.key;

            # /avika/updates/install → frontend (dynamic install script generated by Next.js)
            # Must come BEFORE the general /avika/updates/ block (nginx matches by specificity:
            # exact = match wins over prefix match).
            location = /avika/updates/install {
                proxy_pass http://avika-frontend:443;
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;
            }

            # Routing for Avika Frontend (ncn112.com/avika)
            location /avika {
                proxy_pass http://avika-frontend:443;
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection $connection_upgrade;
            }

            # Routing for Avika Gateway API (ncn112.com/avika/api)
            location /avika/api/ {
                rewrite ^/avika/api/(.*) /api/$1 break;
                proxy_pass http://avika-gateway:443;
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;
            }

            # Routing for Avika Gateway Agent Updates (binaries, deploy script, checksums)
            location /avika/updates/ {
                rewrite ^/avika/updates/(.*) /updates/$1 break;
                proxy_pass http://avika-gateway:443;
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;
            }

            # Routing for Avika Gateway WebSocket Terminal
            location /avika/terminal {
                rewrite ^/avika/terminal /terminal break;
                proxy_pass http://avika-gateway:443;
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection $connection_upgrade;
                proxy_set_header Host $host;
                proxy_read_timeout 3600s;
                proxy_send_timeout 3600s;
            }

            # Route gRPC requests to the Gateway
            # Matches gRPC services for Agent package and standard health checking
            location ~ ^/(nginx\.agent\.v1|grpc\.health\.v1)\. {
                grpc_pass grpc://avika-gateway:8443;
                grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                grpc_read_timeout 1h;
                grpc_send_timeout 1h;
            }

            # Default Redirect for root path
            location / {
                return 302 /avika;
            }
        }
    }
kind: ConfigMap
metadata:
  annotations:
    kubectl.kubernetes.io/last-applied-configuration: |
      {"apiVersion":"v1","data":{"nginx.conf":"user  nginx;\nworker_processes  auto;\n\nerror_log  /var/log/nginx/error.log notice;\npid        /var/run/nginx.pid;\n\nevents {\n    worker_connections  10000;\n}\n\nhttp {\n    include       /etc/nginx/mime.types;\n    default_type  application/octet-stream;\n\n    log_format avika_json escape=json\n    '{'\n      '\"ts\":\"$time_iso8601\",'\n      '\"req_id\":\"$request_id\",'\n      '\"client\":\"$remote_addr\",'\n      '\"method\":\"$request_method\",'\n      '\"path\":\"$uri\",'\n      '\"status\":$status,'\n      '\"bytes\":$body_bytes_sent,'\n      '\"rt\":$request_time'\n    '}';\n\n    access_log  /var/log/nginx/access.log avika_json;\n\n    sendfile        on;\n    keepalive_timeout  65;\n\n    map $http_upgrade $connection_upgrade {\n        default upgrade;\n        ''      close;\n    }\n\n    # K8s DNS Resolver\n    resolver kube-dns.kube-system.svc.cluster.local valid=10s;\n\n    server {\n        listen 443 ssl;\n        http2 on;\n        server_name ncn112.com;\n\n        ssl_certificate     /etc/nginx/certs/tls.crt;\n        ssl_certificate_key /etc/nginx/certs/tls.key;\n\n        # ── /avika/updates/install → frontend (dynamic install script) ──\n        # Must come BEFORE the general /avika/updates/ block.\n        # The frontend generates a self-contained bash script with all\n        # deployment values (UPDATE_SERVER, GATEWAY_SERVER, etc.) baked in.\n        location = /avika/updates/install {\n            proxy_pass http://avika-frontend:443;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n        }\n\n        # Routing for Avika Frontend (ncn112.com/avika)\n        location /avika {\n            # Map to the frontend service (K8s service listens on 443 mapping to container port 5031)\n            proxy_pass http://avika-frontend:443;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            \n            # Handle WebSocket upgrades for Next.js (if applicable)\n            proxy_http_version 1.1;\n            proxy_set_header Upgrade $http_upgrade;\n            proxy_set_header Connection $connection_upgrade;\n        }\n\n        # Routing for Avika Gateway API (ncn112.com/avika/api)\n        location /avika/api/ {\n            rewrite ^/avika/api/(.*) /api/$1 break;\n            proxy_pass http://avika-gateway:443;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n        }\n\n        # Routing for Avika Gateway Agent Updates (binaries, deploy script, etc.)\n        location /avika/updates/ {\n            rewrite ^/avika/updates/(.*) /updates/$1 break;\n            proxy_pass http://avika-gateway:443;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n        }\n\n        # Routing for Avika Gateway WebSocket Terminal\n        location /avika/terminal {\n            rewrite ^/avika/terminal /terminal break;\n            proxy_pass http://avika-gateway:443;\n            proxy_http_version 1.1;\n            proxy_set_header Upgrade $http_upgrade;\n            proxy_set_header Connection $connection_upgrade;\n            proxy_set_header Host $host;\n            proxy_read_timeout 3600s;\n            proxy_send_timeout 3600s;\n        }\n\n        # Route gRPC requests to the Gateway\n        location ~ ^/(nginx\\.agent\\.v1|grpc\\.health\\.v1)\\. {\n            grpc_pass grpc://avika-gateway:8443;\n            grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            grpc_read_timeout 1h;\n            grpc_send_timeout 1h;\n        }\n\n        # Default Redirect for root path\n        location / {\n            return 302 /avika;\n        }\n    }\n}\n"},"kind":"ConfigMap","metadata":{"annotations":{},"name":"avika-nginx-conf","namespace":"avika"}}
    meta.helm.sh/release-name: avika
    meta.helm.sh/release-namespace: avika
  creationTimestamp: "2026-03-28T12:57:51Z"
  labels:
    app.kubernetes.io/instance: avika
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: avika
    app.kubernetes.io/version: 1.109.6
    component: nginx
    helm.sh/chart: avika-1.109.6
  name: avika-nginx-conf
  namespace: avika
  resourceVersion: "6431373"
  uid: 1ab20a89-9c66-4c24-918c-368f24b34f3e
